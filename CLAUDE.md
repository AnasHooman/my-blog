# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code가 따라야 할 지침을 담고 있습니다.

## 프로젝트 개요

마크다운(`.md`) 파일을 읽어 블로그 웹사이트로 보여주는 정적 사이트. 별도 서버나 빌드 단계 없이, 정적 파일 호스팅(GitHub Pages 등)에 그대로 올리면 동작해야 한다.

## 기술 스택 제약

- **프레임워크 없음**: React/Vue/Next 등 UI 프레임워크, 번들러(Webpack/Vite 등), npm 빌드 파이프라인을 쓰지 않는다. 순수 HTML/CSS/JavaScript(ES 모듈)로 구현한다.
- **마크다운 파싱만 예외적으로 경량 라이브러리 허용**: [marked.js](https://marked.js.org/) 같은 파싱 전용 라이브러리는 사용 가능 (CDN 링크 또는 `vendor/`에 단일 파일로 vendoring). 이는 UI 프레임워크가 아니라 파서이므로 "프레임워크 없음" 원칙에 위배되지 않는다.
- 변환은 **브라우저에서 실시간으로** 이루어진다: 페이지 로드 시 JS가 `.md` 파일을 `fetch`해서 HTML로 파싱·렌더링한다. 빌드 스크립트로 미리 정적 HTML을 생성하는 방식은 쓰지 않는다.

## 디렉토리 구조

```
index.html          # 글 목록 페이지
post.html           # 개별 글 렌더링 페이지 (?slug=... 쿼리로 글 지정)
css/style.css        # 전체 스타일 (라이트/다크 테마 변수 포함)
js/main.js           # 글 목록 렌더링, 다크모드 토글
js/post.js           # 개별 글 fetch + 마크다운 파싱 + 렌더링
posts/*.md           # 실제 글 본문 (마크다운)
posts/posts.json     # 글 매니페스트 (아래 참고)
vendor/marked.min.js # (CDN 대신 로컬 사용 시) 마크다운 파서
```

## 글 목록 관리 (`posts/posts.json`)

브라우저에서는 디렉토리 목록을 읽을 수 없으므로, 어떤 글이 존재하는지는 매니페스트 파일로 관리한다.

```json
[
  {
    "slug": "hello-world",
    "title": "첫 글",
    "date": "2026-09-02",
    "summary": "한두 문장 요약"
  }
]
```

**새 글을 추가할 때**: `posts/`에 `.md` 파일을 추가하고, `posts.json`에 해당 항목을 함께 추가한다. 두 단계 모두 빠짐없이 진행해야 글 목록과 상세 페이지에 정상적으로 노출된다.

## 디자인 원칙

- **가독성 우선**: 본문 컨테이너는 읽기 좋은 폭(약 65-75ch)으로 제한하고, 줄 간격·여백을 넉넉히 준다.
- **다크모드**: `prefers-color-scheme`으로 기본값을 감지하고, 사용자가 수동으로 토글할 수 있는 버튼을 둔다. 선택한 테마는 `localStorage`에 저장해 재방문 시 유지한다. 색상은 CSS 커스텀 프로퍼티(`--bg`, `--text`, `--accent` 등)로 정의해 라이트/다크 테마를 한 곳에서 관리한다.
- **모바일 반응형**: 미디어 쿼리로 작은 화면에서 여백·폰트 크기를 조정하고, 뷰포트 메타 태그를 반드시 포함한다. 데스크톱에서 먼저 만들고 나중에 줄이는 대신, 작은 화면에서도 자연스럽게 보이는지 기준으로 확인한다.

## 코드 스타일

- JS는 ES 모듈로 파일을 역할별로 분리한다 (목록 렌더링 / 글 렌더링 / 테마 토글 등).
- 주석은 최소화하고, 코드만으로 드러나지 않는 이유(WHY)가 있을 때만 남긴다.
- CSS는 컴포넌트별로 나누기보다 하나의 `style.css`에서 커스텀 프로퍼티 기반으로 테마를 관리한다 (규모가 커지기 전까지).

## 배포 가정

서버 사이드 로직이 전혀 없다. 파일 그대로 정적 호스팅(GitHub Pages, Netlify 등)에 올리면 동작해야 한다. 로컬에서 확인할 때는 `fetch`가 `file://`에서 CORS로 막히므로 간단한 로컬 서버(`npx serve`, `python -m http.server` 등)로 열어야 한다.
